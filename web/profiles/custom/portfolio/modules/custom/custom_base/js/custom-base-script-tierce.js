Drupal.behaviors.custom_base_bis = {
  attach: function (context, settings) {
    var mavariable = ["Mon test bis"];
    mavariable.push('truc bis');
    console.log("attach de mon custom base bis");
  }
};
