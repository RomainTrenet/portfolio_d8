/**
 * sdnryny,r
 * fh,tru,;,u;
 * @type {{attach: Drupal.behaviors.custom_base.attach}}
 */
Drupal.behaviors.custom_base = {
  attach: function (context, settings) {
    var mavariable = ["sqfbef"];
    mavariable.push('qetrbhrtntyn');
    console.log("attach de mon custom base");
  }
};
